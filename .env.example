# Template des variables d'environnement du bot.
# Copier en .env (gitignore) et remplir les vraies valeurs.

# URL du dashboard Railway (POST /api/ingest)
# Si vide ou non defini -> le bot tourne normalement sans push (mode dev).
DASHBOARD_URL=https://tradingbote-dashboard.up.railway.app/api/ingest
